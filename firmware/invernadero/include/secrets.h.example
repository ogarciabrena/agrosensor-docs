#pragma once
// Copiar como secrets.h y llenar. secrets.h está en .gitignore — NUNCA subirlo.

#define WIFI_SSID          "TU_RED"
#define WIFI_PASS          "TU_PASSWORD"
// En Wokwi usar: SSID "Wokwi-GUEST", PASS ""

#define SUPABASE_URL       "https://xxxxxxxx.supabase.co"
#define SUPABASE_ANON_KEY  "eyJhbGciOi..."   // anon key (Settings -> API). NUNCA el service_role.
